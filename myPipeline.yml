
---

resources:
- name: concourse-pipeline
  type: git
  icon: github
  source:
    uri: https://github.com/mbrown306/Unit10ConcourseLab
    branch: project10
    username: mbrown306
    password: 

---

jobs:
- name: job 
  plan:
  - get: concourse-pipeline
  - task: run-script
    config:
      platform: linux
      image_resource:
        type: registry-image
        source: { repository: ngnix }
      inputs:
        - name: concourse-pipeline
      run:
        path: concourse-pipeline/program.sh 
        args: []
      outputs:
        - name: myfile
